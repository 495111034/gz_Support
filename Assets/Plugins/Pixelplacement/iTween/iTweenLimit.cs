using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class iTweenLimit : MonoBehaviour
{
    // Start is called before the first frame update

    public bool limit = false;

    void Start()
    {
        limit = true;
    }

    // Update is called once per frame
}
